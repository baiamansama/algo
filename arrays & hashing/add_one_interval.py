"""
given an array and q quearies with each having l, r (1 <=l <= r < len(q)).
you should add 1 to each element in l, r interval.

 """

def add_one_interval(arr: [], q: []) -> []:

    for i in range(len(q)):
        



    return []